# Foundry
cache/
out/

# build/ is fully committed (the example's compiled artifacts plus the
# TOOLCHAIN stamp); check-core.sh keeps its intermediates in a temp dir.

# Nix build out-links
result
result-*

# Dotenv
.env
